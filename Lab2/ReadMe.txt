Instruction:
load gcc 6.2 use ”module load gcc-6.2.0" on CIMS
1. cd to lab2 on terminal
2. type "make" to call Makefile
3. type “./lab2 -n <N>“ 
4. type ctrl + c to quit server

Since the server and thread pool will run and wait for request, so the program won’t stop automatically.

Tests are expected in lab 3.